name: Collect CVEs
on:
  workflow_dispatch:
  schedule:
    - cron: '17 */6 * * *'
permissions:
  contents: read
concurrency:
  group: cve-collection
  cancel-in-progress: false
jobs:
  crawl:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: pip install -r requirements.txt
      - run: python scripts/crawl_once.py
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          APPROVAL_FROM_EMAIL: ${{ secrets.APPROVAL_FROM_EMAIL }}
          APPROVAL_RECIPIENT_EMAIL: ${{ secrets.APPROVAL_RECIPIENT_EMAIL }}
          APPROVAL_BASE_URL: https://intelquarry.com
